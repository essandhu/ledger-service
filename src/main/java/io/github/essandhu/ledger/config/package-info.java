/**
 * Spring wiring: security filter chain and JWT role mapping, metrics, {@code java.time.Clock}
 * bean (all production time flows through it, so tests control the clock), OpenAPI, batch
 * infrastructure.
 */
package io.github.essandhu.ledger.config;
