.gradle/
build/
# Root-anchored: IDE output only — must not match src/**/application/port/out/.
/out/

.idea/
*.iml
.vscode/
.DS_Store

.env

# Local planning documents — the public docs are README.md and docs/adr/.
docs/PLAN.md
docs/TEST-STRATEGY.md

# Smoke-probe artifact (CI writes it in the runner workspace only)
app.log

# Playwright install for the README screenshot capture (docs/media/README.md); the lockfile
# beside it IS committed, so a regenerated screenshot comes from the same Playwright.
docs/media/node_modules/
